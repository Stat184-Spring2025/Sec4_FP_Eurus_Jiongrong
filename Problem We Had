1. The dataset website prohibits us from operating the crawler. The table we are going to use can only 
be used by downloading it locally, but this makes it impossible to "share" the data directly with the
code.
